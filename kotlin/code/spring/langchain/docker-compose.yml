services:
  db:
    image: postgres:16
    hostname: postgres
    ports:
      - 5434:5432
    restart: always
    environment:
      POSTGRES_DB: db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pwd
